{
  "name": "lightweight-vulnerability-scanner",
  "version": "1.0.0",
  "description": "A lightweight vulnerability scanner dashboard built with Express and Python.",
  "private": true,
  "scripts": {
    "start": "node backend/src/server.js",
    "backend-dev": "node --watch backend/src/server.js",
    "build": "npm install --prefix backend && npm install --prefix client && npm run build --prefix client"
  }
}
